# API Base URL (leave blank if running locally on same host or using proxy)
VITE_API_BASE_URL=

# Session Warning Threshold in Minutes (Optional FE override, defaults to 5 minutes)
VITE_SESSION_WARNING_MINUTES=5
